{
  "name": "@signature/lambda",
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "serverless offline start",
    "dev:sam": "sam local start-api --host 0.0.0.0 --port 3001",
    "deploy:dev": "serverless deploy --stage dev",
    "test": "jest"
  },
  "dependencies": {
    "@prisma/client": "^5.6.0",
    "@aws-sdk/client-s3": "^3.0.0",
    "@aws-sdk/client-kms": "^3.0.0"
  },
  "devDependencies": {
    "typescript": "^5.2.0",
    "@types/node": "^20.0.0",
    "serverless": "^3.0.0",
    "serverless-plugin-typescript": "^2.1.5",
    "serverless-offline": "^13.0.0"
  }
}